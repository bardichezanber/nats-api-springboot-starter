apiVersion: v1
kind: Service
metadata:
  name: ingest-gateway
  labels:
    app.kubernetes.io/name: ingest-gateway
    app.kubernetes.io/part-of: nats-ingest
spec:
  selector:
    app.kubernetes.io/name: ingest-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
