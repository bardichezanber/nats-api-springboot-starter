apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Renames every base resource: ingest-worker -> ingest-worker-route-a.
nameSuffix: -route-a
resources:
  - ../../base/worker
  - scaledobject.yaml
patches:
  - path: sources-patch.yaml
